import { defineConfig } from 'astro/config';

// https://astro.build
export default defineConfig({
  // Set this to your deployed URL, e.g. 'https://ruipereira.dev'
  site: 'https://example.com',
});
